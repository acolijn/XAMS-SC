# Editing dashboards

Dashboards are edited in the browser and then **saved back into the
repository** with `tools/save_dashboard.py`, so that an edit made at two in the
morning is not lost when the machine is reinstalled.

!!! warning "Not written yet"
    Wanted: the round trip, step by step, and the panel conventions worth keeping.
