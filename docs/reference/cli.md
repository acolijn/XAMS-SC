# Command line

    xams-ctl status
    xams-ctl reload

!!! warning "Not written yet"
    Wanted: every subcommand, its options and its exit codes. Should be generated from the parser rather than typed, on the same argument as the [channel](channels.md) and [alarm](alarms.md) tables.
