# Storage and sinks

Three subscribers write: the JSONL archive, PostgreSQL, and the alarm log.

!!! warning "Not written yet"
    Wanted: the archive layout, `sql/schema.sql`, retention, and the replay procedure that makes the database disposable.
