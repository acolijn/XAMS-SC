# Topic contract

The MQTT topic layout, the payload schema, retained topics and heartbeats.

!!! warning "Not written yet"
    Source material: [the design specification](../DESIGN.md) §3 and `src/xams_sc/bus.py`. This is the interface every driver and every sink agrees on, so it deserves a page of its own rather than a section inside a longer one.
