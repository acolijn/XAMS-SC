# Writing a new driver

!!! warning "Not written yet"
    Wanted: a worked example, start to finish — add the channels to `channels.yaml`, subclass the service, publish, register it in `devices/__main__.py`, add a simulation mode, write the test. This is the page that decides whether the system outlives its author.
